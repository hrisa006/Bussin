<!DOCTYPE html>
<html lang="bg">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Map</title>
    <link rel="stylesheet" href="/styles.css" />
    <script
      src="https://maps.googleapis.com/maps/api/js?key=<%= process.env.API_KEY %>&callback=initMap"
      async
      defer
    ></script>
  </head>
  <body>
    <h1>Map of Bus Urban Mobility</h1>
    <div id="map" style="height: 500px; width: 100%"></div>

    <h2>Near-me bus stops and their routes:</h2>
    <ul id="stops-list"></ul>

    <h2>Make an alert</h2>
    <form id="notification-form">
      <label for="stop">Choose bus stop:</label>
      <select id="stop" name="stop" required></select>
      <br />
      <label for="route">Choose bus route:</label>
      <select id="route" name="route" required></select>
      <br />
      <label for="minutes">Alert me before (min):</label>
      <input type="number" id="minutes" name="minutes" min="1" required />
      <br />
      <button type="submit">Set an Alert</button>
    </form>

    <script src="/scripts.js"></script>
  </body>
</html>
